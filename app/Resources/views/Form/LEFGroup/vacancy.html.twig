{# app/Resources/views/Form/LEFGroup/vacancy.html.twig #}

{{ form_start(form) }}
	
	{{ form_errors(form) }}
	
	{{ form_row(form.vacancies) }}
	
	
	<div class="form-group row">
		<div class="offset-sm-2 col-sm-10">
			{{ form_widget(form.save) }}
		</div>
	</div>
	{{ form_rest(form) }}
	
{{ form_end(form) }}